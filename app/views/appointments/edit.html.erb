<h2>Edit Appointment ID#<%= @appointment.id %></h2>

<%= render partial: 'form', locals: {submit_tag: 'Save'} %>

<div class="delete">
    <p><%= button_to 'Delete', appointment_path(@appointment), method: :delete, data: {confirm: "Are you sure you want to delete 'Appointment ID##{@appointment.id}'?"} %></p>
</div>