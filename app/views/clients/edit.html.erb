<h3>Edit Client #<%= @client.id %></h3>

<%= render partial: 'form', locals: {submit_tag: "Save"} %>

<br>
<%= button_to 'Delete', service_path(@client), method: :delete, data: {confirm: "Are you sure you want to delete '#{@client.name}'?"} %>
