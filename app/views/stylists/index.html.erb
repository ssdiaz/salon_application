<h2>View All Stylists</h2>

<ul>
<% @stylists.each do |stylist| %>
    <li>
        <%= link_to stylist.name, stylist_path(stylist) %>, Level: <%= stylist.level%>
    </li>
<% end %>
</ul>

<%= link_to 'Create a New Stylist', new_stylist_path if logged_in? %>
