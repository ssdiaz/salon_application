<h2>Edit User Profile:</h2>

<%= render partial: 'form', locals: {submit_tag: 'Save'} %>

<div class="delete">
    <p><%= button_to 'Delete', user_path(@user), method: :delete, data: {confirm: "Are you sure you want to delete '#{@user.name}'?"} %></p>
</div>